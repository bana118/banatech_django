https-portal:
  image: steveltn/https-portal:1
  ports:
    - '80:80'
    - '443:443'
  links:
    - django
  restart: always
  environment:
    DOMAINS: 'django.dip.jp -> http://django'
    # STAGE: 'production'
    # FORCE_RENEW: 'true'

django:
  image: django
  